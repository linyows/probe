name: Embedded Failure Test

jobs:
- name: Test Job
  steps:
  - name: Failure embedded job
    id: embedded_test
    uses: embedded
    with:
      path: "./e2e/embedded-failure-job.yml"
    test: res.code == 0