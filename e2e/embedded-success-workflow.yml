name: Embedded Success Test

jobs:
- name: Test Job
  steps:
  - name: Success embedded job
    id: embedded_test
    uses: embedded
    with:
      path: "./e2e/embedded-success-job.yml"
    test: res.code == 0