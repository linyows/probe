name: Success Workflow (Hello Action)
description: Simple workflow that should succeed using hello action
jobs:
- name: test-job
  steps:
  - name: Success Step
    uses: hello
    with:
      name: World
    test: true