name: Success Workflow (Localhost)
description: Simple workflow that should succeed using localhost
jobs:
- name: test-job
  steps:
  - name: Success Step
    uses: http
    with:
      url: http://localhost:PORT_PLACEHOLDER
      get: /status/200
    test: res.code == 200